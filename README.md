# AssignmentBuildUrlShorter
For Frontend port http://localhost:3000
For Backend port running on http://localhost:8080
For Running backend server needs Java 17 version 
For Running frontend server run yarn start
